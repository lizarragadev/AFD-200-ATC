import 'package:flutter/material.dart';

class ListViewListilesScreen extends StatelessWidget {
  const ListViewListilesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
